# /etc/sysctl.d/99-tuxdns-security.conf
# Linux Network Stack Hardening for TuxDNS-Hole Daemon

# 1. Reverse Path Filtering (Anti-IP Spoofing)
net.ipv4.conf.all.rp_filter = 1
net.ipv4.conf.default.rp_filter = 1

# 2. Protect TCP sockets from TIME-WAIT assassination (RFC 1337)
net.ipv4.tcp_rfc1337 = 1

# 3. BPF JIT Hardening against memory spraying/manipulation
net.core.bpf_jit_harden = 2

# 4. Disable ICMP Redirect Acceptance (Prevents MITM routing tampering)
net.ipv4.conf.all.accept_redirects = 0
net.ipv4.conf.default.accept_redirects = 0
net.ipv6.conf.all.accept_redirects = 0
net.ipv6.conf.default.accept_redirects = 0

# 5. Disable ICMP Redirect Sending
net.ipv4.conf.all.send_redirects = 0
net.ipv4.conf.default.send_redirects = 0

# 6. Ignore ICMP Echo Broadcasts (Smurf Attack Protection)
net.ipv4.icmp_echo_ignore_broadcasts = 1

# 7. Protect against SYN Floods
net.ipv4.tcp_syncookies = 1
net.ipv4.tcp_max_syn_backlog = 4096
